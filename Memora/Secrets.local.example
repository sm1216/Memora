# Copy to Secrets.local (gitignored) for server/CLI tools only.
# NEVER paste sk.* into Swift that ships on the iPhone.

MAPBOX_PUBLIC_TOKEN=pk.YOUR_PUBLIC_TOKEN
MAPBOX_SECRET_TOKEN=sk.YOUR_SECRET_TOKEN
